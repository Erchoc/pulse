{
  "name": "pulse",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "dev": "concurrently -n server,web -c cyan,yellow \"pnpm dev:server\" \"pnpm dev:web\"",
    "dev:server": "make -C packages/server dev",
    "dev:web": "pnpm --filter @pulse/web dev",
    "build": "pnpm --filter @pulse/web build && make -C packages/server build",
    "build:probe": "make -C packages/probe build",
    "start": "make -C packages/server dev",
    "ship": "fly deploy",
    "pretest": "mkdir -p test-reports",
    "test": "pnpm --filter @pulse/web test && make -C packages/server test && make -C packages/probe test",
    "test:go": "make -C packages/server test && make -C packages/probe test",
    "test:unit": "pnpm --filter @pulse/web test:unit",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "typecheck": "pnpm --filter @pulse/web typecheck",
    "verify": "pnpm lint && pnpm typecheck && pnpm test && pnpm build"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "concurrently": "^9.1.0",
    "lefthook": "^1.11.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["@biomejs/biome", "esbuild", "lefthook"]
  }
}
