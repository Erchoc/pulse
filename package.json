{
  "name": "pulse",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "dev": "concurrently -n server,web -c cyan,yellow \"pnpm dev:server\" \"pnpm dev:web\"",
    "dev:server": "pnpm --filter @pulse/server dev",
    "dev:web": "pnpm --filter @pulse/web dev",
    "build": "pnpm --filter @pulse/web build && pnpm --filter @pulse/server build",
    "start": "pnpm --filter @pulse/server start",
    "ship": "fly deploy",
    "test": "pnpm -r test",
    "test:unit": "pnpm -r test:unit",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "typecheck": "pnpm -r typecheck",
    "verify": "pnpm lint && pnpm typecheck && pnpm test && pnpm build"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "concurrently": "^9.1.0",
    "lefthook": "^1.11.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["@biomejs/biome", "esbuild", "lefthook"]
  }
}
