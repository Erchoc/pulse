{
  "name": "pulse",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "dev": "concurrently -n go-server,web -c cyan,yellow \"pnpm dev:go\" \"pnpm dev:web\"",
    "dev:go": "make -C packages/go-server dev",
    "dev:web": "pnpm --filter @pulse/web dev",
    "dev:server": "pnpm --filter @pulse/server dev",
    "build": "pnpm --filter @pulse/web build && make -C packages/go-server build",
    "build:probe": "make -C packages/probe build",
    "start": "make -C packages/go-server dev",
    "ship": "fly deploy",
    "test": "pnpm -r test && make -C packages/go-server test && make -C packages/probe test",
    "test:go": "make -C packages/go-server test && make -C packages/probe test",
    "test:unit": "pnpm -r test:unit",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "typecheck": "pnpm -r typecheck",
    "verify": "pnpm lint && pnpm typecheck && pnpm test && pnpm build"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "concurrently": "^9.1.0",
    "lefthook": "^1.11.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["@biomejs/biome", "esbuild", "lefthook"]
  }
}
